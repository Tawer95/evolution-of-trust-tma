# Netlify deploy config — статика без билда.
# webapp/ публикуется как корень сайта; относительные пути ассетов (css/, js/) резолвятся от корня.
[build]
  publish = "webapp"
  # command намеренно не задан: билд-системы нет, отдаём файлы как есть.
